/**
 * @file src/constants.js
 * @description Единый источник правды для констант, статусов и настроек.
 * @module Constants
 */

// =============================================================================
// 👥 РОЛИ И ДОСТУПЫ (RBAC)
// =============================================================================
export const ROLES = {
    ADMIN: 'admin',      // Владелец: Всё может
    MANAGER: 'manager',  // Сотрудник: Управление заказами, личная касса
    CLIENT: 'client'     // Клиент: Только свои заказы
};

// =============================================================================
// 🔌 СТАТУСЫ ЗАКАЗОВ
// =============================================================================
export const ORDER_STATUS = {
    NEW: 'new',         // Только создан
    DISCUSS: 'discuss', // Менеджер взял, звонит
    WORK: 'work',       // В работе (черновая/чистовая)
    DONE: 'done',       // Сдан и оплачен
    CANCEL: 'cancel'    // Отменен
};

export const STATUS_LABELS = {
    [ORDER_STATUS.NEW]: '🆕 Новый',
    [ORDER_STATUS.DISCUSS]: '🗣 Обсуждение',
    [ORDER_STATUS.WORK]: '🛠 В работе',
    [ORDER_STATUS.DONE]: '✅ Сдан',
    [ORDER_STATUS.CANCEL]: '❌ Отмена'
};

// =============================================================================
// 💰 ПРАЙС-ЛИСТ (PRICING ENGINE)
// =============================================================================
// Цены взяты средние из твоего диапазона для базового расчета.
// Их можно будет редактировать через Админку (база данных перекроет эти значения).
export const PRICING = {
    // === ЧЕРНОВЫЕ РАБОТЫ ===
    rough: {
        strobeConcrete: 1750,      // Штробление (Бетон) 1500-2000
        strobeBrick: 1100,         // Штробление (Кирпич) 1000-1200
        cableLaying: 400,          // Прокладка кабеля 300-500
        drillHoleConcrete: 1500,   // Высверливание лунки (Бетон) FIX
        drillHoleBrick: 1000,      // Высверливание лунки (Кирпич) FIX
        socketBoxInstall: 600,     // Вмазка подрозетника 500-700
        junctionBoxAssembly: 3000  // Сборка распредкоробки 2500-3500
    },

    // === ЧИСТОВЫЕ РАБОТЫ ===
    finish: {
        socketInstall: 1000,       // Установка розетки/выключателя 800-1200
        shieldModule: 1750,        // Сборка щита (за модуль) 1500-2000
        lampInstall: 5000,         // Установка люстры 3000-7000+
        ledStrip: 2000             // Монтаж LED-ленты 1500-2500
    },

    // === МАТЕРИАЛЫ ===
    materialsFactor: 0.45          // Коэфф. материалов (45% от стоимости работ)
};

// =============================================================================
// 🧮 ПРАВИЛА РАСЧЕТА (ESTIMATE RULES)
// =============================================================================
export const ESTIMATE_RULES = {
    // Эвристика: сколько штук/метров обычно уходит на 1 кв.м площади
    cablePerSqm: 3.5,        // ~3.5м кабеля на м2 (было 6.5, уменьшил для реалистичности)
    strobePerSqm: 0.8,       // ~0.8м штробы на м2
    pointsPerSqm: 0.6,       // Точек на м2
    minShieldModules: 12     // Минимальный щит
};

// =============================================================================
// ⌨️ КЛАВИАТУРЫ (UI LAYOUTS)
// =============================================================================
export const KEYBOARDS = {
    main: (role) => {
        const btns = [
            [{ text: "🧮 Рассчитать стоимость" }, { text: "📂 Мои заказы" }],
            [{ text: "💰 Прайс-лист" }, { text: "📞 Контакты" }]
        ];
        
        if (role === ROLES.MANAGER || role === ROLES.ADMIN) {
            btns.unshift([{ text: "👷‍♂️ Мои объекты (Активные)" }]);
        }
        
        if (role === ROLES.ADMIN) {
            btns.unshift([{ text: "👑 Админ-панель" }]);
        }
        
        return { keyboard: btns, resize_keyboard: true };
    },

    admin: {
        keyboard: [
            [{ text: "📊 Статистика" }, { text: "⚙️ Настройки цен" }], // Добавил кнопку настроек
            [{ text: "👥 Сотрудники" }, { text: "🔙 Главное меню" }]
        ],
        resize_keyboard: true
    },

    cancel: {
        keyboard: [[{ text: "❌ Отмена" }]],
        resize_keyboard: true
    },

    walls: {
        inline_keyboard: [
            [{ text: "🧱 Кирпич / Блок", callback_data: "calc_wall_brick" }],
            [{ text: "🏗 Бетон / Монолит", callback_data: "calc_wall_concrete" }]
        ]
    }
};

// =============================================================================
// 💬 ТЕКСТЫ (TEMPLATES)
// =============================================================================
export const TEXTS = {
    priceList: () => 
        `📋 <b>ОФИЦИАЛЬНЫЙ ПРАЙС-ЛИСТ 2026</b>\n` +
        `➖➖➖➖➖➖➖➖➖➖\n` +
        `<b>🏗 ЧЕРНОВЫЕ РАБОТЫ:</b>\n` +
        `▫️ Штробление (Бетон): ${PRICING.rough.strobeConcrete} ₸/м\n` +
        `▫️ Штробление (Кирпич): ${PRICING.rough.strobeBrick} ₸/м\n` +
        `▫️ Прокладка кабеля: ${PRICING.rough.cableLaying} ₸/м\n` +
        `▫️ Высверливание (Бетон): ${PRICING.rough.drillHoleConcrete} ₸/шт\n` +
        `▫️ Высверливание (Кирпич): ${PRICING.rough.drillHoleBrick} ₸/шт\n` +
        `▫️ Вмазка подрозетника: ${PRICING.rough.socketBoxInstall} ₸/шт\n` +
        `▫️ Сборка распредкоробки: ${PRICING.rough.junctionBoxAssembly} ₸/шт\n\n` +
        
        `<b>✨ ЧИСТОВЫЕ РАБОТЫ:</b>\n` +
        `▫️ Установка розетки/выкл: ${PRICING.finish.socketInstall} ₸/шт\n` +
        `▫️ Сборка щита (модуль): ${PRICING.finish.shieldModule} ₸/шт\n` +
        `▫️ Люстры/Бра: от ${PRICING.finish.lampInstall} ₸\n` +
        `▫️ LED лента: ${PRICING.finish.ledStrip} ₸/м\n\n` +
        
        `<i>❗️ Цены ориентировочные и могут меняться в зависимости от сложности.</i>`
};