/**
 * @file src/database/connection.js
 * @description Модуль управления соединениями с PostgreSQL.
 * Реализует паттерн "Connection Pool" для эффективного переиспользования соединений.
 * @module DatabaseConnection
 * @version 1.0.0 (Senior Level)
 */

import pg from "pg";
import { config } from "../config.js"; // Предполагаем наличие конфига

// Деструктуризация для чистоты кода
const { Pool } = pg;

// =============================================================================
// КОНФИГУРАЦИЯ ПУЛА (POOL CONFIGURATION)
// =============================================================================
const poolConfig = {
  // Используем готовую строку со всеми данными
  connectionString: config.db.connectionString, 
  
  // SSL настройки для продакшена
  ssl: config.db.ssl, 
  
  max: config.db.max || 20,
  idleTimeoutMillis: config.db.idleTimeoutMillis || 30000,
  connectionTimeoutMillis: config.db.connectionTimeoutMillis || 5000,
};

// Создаем единственный экземпляр пула на все приложение (Singleton)
const pool = new Pool(poolConfig);

// =============================================================================
// МОНИТОРИНГ СОСТОЯНИЯ (POOL EVENTS)
// =============================================================================

// Событие срабатывает при создании каждого нового клиента в пуле
pool.on("connect", () => {
  // В продакшене здесь можно добавить метрику для Grafana/Prometheus
  // console.log("📦 [DB POOL] Создано новое соединение с базой данных.");
});

// Событие срабатывает при ошибке неактивного клиента
pool.on("error", (err) => {
  console.error(
    "🔥 [DB POOL ERROR] Внезапная ошибка клиента базы данных:",
    err,
  );
  // Не выбрасываем ошибку здесь, чтобы не положить всё приложение,
  // пул сам попытается пересоздать соединение.
});

// =============================================================================
// ПУБЛИЧНЫЕ МЕТОДЫ (PUBLIC METHODS)
// =============================================================================

/**
 * Получение клиента из пула для выполнения транзакций.
 * ⚠️ ВАЖНО: После использования клиента ОБЯЗАТЕЛЬНО нужно вызвать client.release(),
 * иначе соединение зависнет и пул переполнится.
 *
 * @returns {Promise<import('pg').PoolClient>} Клиент PostgreSQL
 */
export const getClient = async () => {
  try {
    const client = await pool.connect();
    return client;
  } catch (error) {
    console.error("❌ [DB CONNECTION] Не удалось получить клиента из пула.");
    throw error;
  }
};

/**
 * Выполнение одиночного SQL-запроса (Shortcut).
 * Автоматически берет клиента, выполняет запрос и возвращает клиента в пул.
 * Подходит для простых операций без транзакций.
 *
 * @param {string} text - SQL-запрос
 * @param {Array<any>} params - Параметры запроса
 * @returns {Promise<import('pg').QueryResult>} Результат запроса
 */
export const query = async (text, params) => {
  const start = Date.now();
  try {
    const res = await pool.query(text, params);

    // Логирование медленных запросов (например, дольше 100мс)
    const duration = Date.now() - start;
    if (duration > 100) {
      console.warn(`⚠️ [DB SLOW QUERY] Запрос занял ${duration}мс: ${text}`);
    }

    return res;
  } catch (error) {
    console.error("❌ [DB QUERY ERROR] Ошибка выполнения запроса:", {
      text,
      error,
    });
    throw error;
  }
};

/**
 * Закрытие пула соединений.
 * Используется при Graceful Shutdown (остановке сервера).
 */
export const closePool = async () => {
  await pool.end();
  console.log("🔌 [DB POOL] Пул соединений закрыт.");
};
